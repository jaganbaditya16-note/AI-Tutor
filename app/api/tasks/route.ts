import {NextResponse} from 'next/server';import{requireUser}from '@/lib/auth';import{db}from '@/lib/db';
async function owned(projectId:string,userId:string){const{data}=await db().from('projects').select('id').eq('id',projectId).eq('user_id',userId).single();return !!data}
export async function GET(req:Request){try{const u=await requireUser(),id=new URL(req.url).searchParams.get('projectId');if(!id||!(await owned(id,u)))return NextResponse.json({error:'Project not found'},{status:404});const{data,error}=await db().from('tasks').select('*').eq('project_id',id).order('created_at',{ascending:false});if(error)throw error;return NextResponse.json({tasks:data||[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:500})}}
export async function POST(req:Request){try{const u=await requireUser(),b=await req.json();if(!b.projectId||!(await owned(b.projectId,u)))return NextResponse.json({error:'Project not found'},{status:404});const title=String(b.title||'').trim();if(!title)return NextResponse.json({error:'Title required'},{status:400});const{data,error}=await db().from('tasks').insert({project_id:b.projectId,title,description:b.description||null,priority:b.priority||'Medium'}).select().single();if(error)throw error;return NextResponse.json({task:data},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:500})}}
export async function PATCH(req:Request){try{const u=await requireUser(),b=await req.json();const{data:t}=await db().from('tasks').select('*,projects!inner(user_id)').eq('id',b.id).eq('project_id',b.projectId).single();if(!t||t.projects?.user_id!==u)return NextResponse.json({error:'Not found'},{status:404});const{data,error}=await db().from('tasks').update({status:b.status}).eq('id',b.id).select().single();if(error)throw error;return NextResponse.json({task:data})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Failed'},{status:500})}}
